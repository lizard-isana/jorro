# Icon Source

Place your icon source image here as:

- `icon-source.png`

Recommended:

- square image (`1024x1024` or larger)
- transparent background if possible

Build scripts will generate:

- `dist/icons/jorro.icns` (macOS)
- `dist/icons/jorro.ico` (Windows)
